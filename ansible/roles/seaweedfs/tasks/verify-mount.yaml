---
- name: Require a mounted SeaweedFS data directory
  ansible.builtin.command:
    argv:
      - /usr/bin/mountpoint
      - --quiet
      - "{{ seaweedfs_data_directory }}"
  changed_when: false
  check_mode: false
