# A single token with:
#   Zone / Zone / Read
#   Zone / DNS / Edit
# Scoped only to canhdinh.com.
CF_DNS_API_TOKEN={{ cloudflare_api_token }}

# Optional provider tuning.
CLOUDFLARE_PROPAGATION_TIMEOUT=180
CLOUDFLARE_POLLING_INTERVAL=5
CLOUDFLARE_TTL=120
